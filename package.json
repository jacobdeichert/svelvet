{
  "name": "sveltepack",
  "description": "A cli svelte compiler that works with snowpack",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "cd test && npm install && npm run build:prod",
    "format": "prettier '**/*.{js,jsx,ts,tsx,css,html,json,yml}' --write",
    "lint": "eslint . --ignore-pattern dist"
  },
  "dependencies": {
    "chokidar": "^3.3.1",
    "p-debounce": "^2.1.0",
    "snowpack": "^1.0.5"
  },
  "devDependencies": {
    "@jakedeichert/eslint-config": "^4.0.5",
    "prettier": "^1.16.1",
    "svelte": "^3.7.1"
  },
  "peerDependencies": {
    "svelte": "^3.7.1"
  }
}
