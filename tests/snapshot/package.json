{
  "name": "svelvet-test",
  "private": true,
  "scripts": {
    "dev": "npm run clean && svelvet",
    "build": "NODE_ENV=production npm run dev",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "svelte": "3.18.1"
  },
  "devDependencies": {
    "svelvet": "file:../.."
  }
}
