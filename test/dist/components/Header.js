/* generated by Svelte v3.17.1 */
import { SvelteComponent, append, attr, detach, element, init, insert, noop, safe_not_equal } from "/web_modules/svelte/internal.js";

function add_css() {
  var style = element("style");
  style.id = "svelte-nwbabr-style";
  style.textContent = "h1.svelte-nwbabr{color:#000;font-size:70px}.header.svelte-nwbabr{max-width:800px}";
  append(document.head, style);
}

function create_fragment(ctx) {
  let div;
  let h1;
  return {
    c() {
      div = element("div");
      h1 = element("h1");
      h1.textContent = `${HEADER_NAME}`;
      attr(h1, "class", "svelte-nwbabr");
      attr(div, "class", "header svelte-nwbabr");
    },

    m(target, anchor) {
      insert(target, div, anchor);
      append(div, h1);
    },

    p: noop,
    i: noop,
    o: noop,

    d(detaching) {
      if (detaching) detach(div);
    }

  };
}

const HEADER_NAME = "sveltepackkkkkkk";

class Component extends SvelteComponent {
  constructor(options) {
    super();
    if (!document.getElementById("svelte-nwbabr-style")) add_css();
    init(this, options, null, create_fragment, safe_not_equal, {});
  }

}

export default Component;